name: disaster_alert_app
description: Sentinel Suraksha - India's Leading Hazard Detection Platform
publish_to: 'none'
version: 1.2.3+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.5.3
  geolocator: ^10.1.0
  http: ^1.1.0
  url_launcher: ^6.2.4
  percent_indicator: ^4.2.3
  image_picker: ^1.0.7
  cupertino_icons: ^1.0.2
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true